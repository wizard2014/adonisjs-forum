@layout('layouts/default')

@section('content')
  @set('posts', posts.toJSON())

  <div class="box">
    <div class="section">
      @if(posts.length)
        @each(post in posts)
          @include('forum.partials._post')
        @endeach
      @else
        <p>There are no posts</p>
      @endif
    </div>
  </div>
@endsection
